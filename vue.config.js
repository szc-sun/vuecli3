var path = require('path');
module.exports = {
    outputDir:'vuecli3',
    publicPath: './',
    devServer: {
      // 设置主机地址
      host: 'localhost',
      // 设置默认端口
      port: '8080',
      // 打开浏览器
      open: true,
      port: 9000,
    }
}